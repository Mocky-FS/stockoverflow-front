import React from 'react';
import PropTypes from 'prop-types';

const Settings = props => {
    return (
        <div>
                <h3>Réglages</h3>
        </div>
    );
};

Settings.propTypes = {
    
};

export default Settings;